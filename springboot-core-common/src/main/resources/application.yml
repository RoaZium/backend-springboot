server:
  port: ${SERVER_PORT:38080}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:mariadb}

springdoc:
  swagger-ui:
    path: /swagger
    enabled: ${SWAGGER_UI_ENABLED:true}
  api-docs:
    path: /v3/api-docs